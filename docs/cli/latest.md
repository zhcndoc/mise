<!-- @generated by usage-cli from usage spec -->
# `mise latest`

- **用法**: `mise latest [-i --installed] [--minimum-release-age <MINIMUM_RELEASE_AGE>] <TOOL@VERSION>`
- **源代码**: [`src/cli/latest.rs`](https://github.com/jdx/mise/blob/main/src/cli/latest.rs)

获取某个插件可用的最新版本

支持诸如 `node@20` 这样的前缀，以获取 node 20 的最新版本。

## 参数

### `<TOOL@VERSION>`

用于获取最新版本的工具

## 标志

### `-i --installed`

显示最新已安装版本，而不是可用版本

### `--minimum-release-age <MINIMUM_RELEASE_AGE>`

只考虑在此日期之前发布或早于此时长的版本

支持类似 "2024-06-01" 的绝对日期，以及类似 "90d" 或 "1y" 的相对时长。
覆盖每个工具的 `minimum_release_age` 选项以及全局 `minimum_release_age` 设置。

示例：

```
$ mise latest node@20  # 获取 node 20 的最新版本
20.0.0

$ mise latest node     # 获取 node 的最新稳定版本
20.0.0

$ mise latest node --minimum-release-age 2024-01-01  # 在 2024-01-01 之前发布的最新稳定 node
```
