---
description: "列出注册表简写名称及其后端"
---

<!-- @generated by usage-cli from usage spec -->
# `mise registry`

- **用法：** `mise registry [FLAGS] [NAME]`
- **效果：** 只读
- **源代码：** [`src/cli/registry.rs`](https://github.com/jdx/mise/blob/main/src/cli/registry.rs)

列出注册表简写名称及其后端

注册表将简短名称映射到安装后端。例如，`node` 使用内置的 Node 后端。一个工具可能有多个候选项；显式后端语法和配置可以覆盖注册表选择。

这不是 mise 可以安装的所有工具的列表。对于受支持但没有注册表简写的源，请使用类似 `github:owner/repo` 的显式标识符。

## 参数
- **`[NAME]`** — 仅显示指定工具的完整名称

## Flags
- **`-b --backend <BACKEND>`** — 仅显示此后端的工具
- **`--hide-aliased`** — 隐藏别名工具
- **`-J --json`** — 以 JSON 格式输出
- **`--security`** — 在 JSON 输出中包含每个工具后端的安全特性

  需要使用 --json。安全信息会在工具的所有后端之间去重。由于每个后端的安全信息都需要单独解析，对于大型列表，这可能会显著增加耗时。
- **`-h --help`** — 打印帮助

## Examples

列出注册表，然后检查 node。第二条命令输出 `core:node`。

```
mise registry
mise registry node
```

```
mise registry --backend aqua
mise registry --json
```

<!-- generated reference navigation -->

## 相关文档

- [注册表和显式后端](/registry.html)。
- [所有命令](/cli/)。
- [全局标志和参数语法](/cli/#global-flags)。
