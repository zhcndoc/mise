---
description: "永不捕获匹配 glob 的路径"
---

<!-- @generated by usage-cli from usage spec -->
# `mise dotfiles exclude`

- **用法：** `mise dotfiles exclude <GLOB>`
- **效果：** 修改状态
- **源代码：** [`src/cli/dotfiles/exclude.rs`](https://github.com/jdx/mise/blob/main/src/cli/dotfiles/exclude.rs)

永不捕获匹配 glob 的路径

将 glob 添加到全局配置中的 `[history] exclude`。将其用于日志、缓存、数据库和不断重写的应用程序状态；对于确实包含配置但经常变化的文件，最好使用 `--no-autosave` 跟踪并显式保存

## 参数
- **`<GLOB>`** — 例如 `~/.config/hypr/plugins/**` 这样的 glob

## 标志
- **`-h --help`** — 打印帮助

<!-- generated reference navigation -->

## 相关文档

- [开始使用](/getting-started.html)。
- [`mise dotfiles <SUBCOMMAND>`](/cli/dotfiles.html)。
- [全局标志和参数语法](/cli/#global-flags)。
