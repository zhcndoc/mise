---
description: "生成一个 GitHub Action 工作流文件"
---

<!-- @generated by usage-cli from usage spec -->
# `mise generate github-action`

- **用法：** `mise generate github-action [FLAGS]`
- **效果：** 修改状态
- **源代码：** [`src/cli/generate/github_action.rs`](https://github.com/jdx/mise/blob/main/src/cli/generate/github_action.rs)

生成一个 GitHub Action 工作流文件

此命令会生成一个 GitHub Action 工作流文件，在拉取请求、标签、手动调度以及推送到当前 Git 分支时运行类似 `mise run ci` 的 mise 任务
默认打印 YAML；`--write` 会将其保存到 .github/workflows 下。定义所选任务，并在提交前检查生成的触发器

## 选项
- **`-t --task <TASK>`** — 工作流触发时运行的任务

  **默认值：** `ci`
- **`-w --write`** — 写入 .github/workflows/$name.yml
- **`--name <NAME>`** — 要生成的工作流名称

  **默认值：** `ci`
- **`-h --help`** — 打印帮助

## 示例

写入工作流前进行预览

```
mise generate github-action --task=ci
mise generate github-action --write --task=ci
git add .github/workflows/ci.yml
```

<!-- generated reference navigation -->

## 相关文档

- [持续集成](/continuous-integration.html)。
- [`mise generate <SUBCOMMAND>`](/cli/generate.html)。
- [全局标志和参数语法](/cli/#global-flags)。
