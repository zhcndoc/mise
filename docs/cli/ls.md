---
description: "列出已安装和已激活的工具版本"
---

<!-- @generated by usage-cli from usage spec -->
# `mise ls`

- **用法：** `mise ls [FLAGS] [INSTALLED_TOOL]…`
- **别名：** `list`
- **效果：** 只读
- **源代码：** [`src/cli/ls.rs`](https://github.com/jdx/mise/blob/main/src/cli/ls.rs)

列出已安装和已激活的工具版本

列出 mise 了解的工具：已安装的版本，以及配置文件请求的版本（已激活），无论这些版本是否已安装。

## 参数
- **`[INSTALLED_TOOL]…`** — 仅显示来自 [TOOL] 的工具版本

## 标志
- **`--truncate`** — 截断过长的终端输出以适应可用宽度

  **默认值：** `true`
- **`-c --current`** — 仅显示当前在 mise.toml 中指定的工具版本
- **`-g --global`** — 仅显示当前在全局 mise.toml 中指定的工具版本
- **`-i --installed`** — 仅显示已安装的工具版本（隐藏 mise.toml 中定义但未安装的工具）
- **`-J --json`** — 以 JSON 格式输出
- **`-l --local`** — 仅显示当前在本地 mise.toml 中指定的工具版本
- **`-m --missing`** — 显示缺失的工具版本
- **`--all-sources`** — 显示工具的所有已跟踪配置源
- **`--monorepo`** — 列出每个 [monorepo].config_roots 配置根中的工具

  使用活动的 MISE_ENV，并要求设置 monorepo_root = true，且在 monorepo 根配置中显式设置
  [monorepo].config_roots。

  **环境变量：** `MISE_MONOREPO`
- **`--no-header`** — 不显示标题
- **`--outdated`** — 显示版本是否已过时
- **`--prefix <PREFIX>`** — 显示匹配此前缀的版本
- **`--prunable`** — 仅列出可使用 `mise prune` 清理的工具
- **`-h --help`** — 打印帮助

## 示例

显示已安装的版本和活动配置中的请求

```
mise ls
```

仅显示当前配置请求的版本

```
mise ls --current
```

查找需要安装的已配置版本

```
mise ls --missing
```

机器可读的输出：以工具名称为键的对象

```
mise ls --json
```

提供工具参数时，JSON 输出为其版本记录的数组

```
mise ls node --json
```

包含每个已跟踪配置文件中的引用

```
mise ls --all-sources
```

<!-- generated reference navigation -->

## 相关文档

- [开发工具](/dev-tools/)。
- [所有命令](/cli/)。
- [全局标志和参数语法](/cli/#global-flags)。
