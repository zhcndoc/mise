---
description: "发布、获取并记录当前待处理的内容"
---

<!-- @generated by usage-cli from usage spec -->
# `mise dotfiles sync`

- **用法：** `mise dotfiles sync [FLAGS]`
- **效果：** 修改状态
- **源代码：** [`src/cli/dotfiles/sync.rs`](https://github.com/jdx/mise/blob/main/src/cli/dotfiles/sync.rs)

发布、获取并记录当前待处理的内容

获取 origin 分支并发布普通的本地提交历史。
被拒绝的推送会再次获取，并在不重写历史的情况下进行协调。
记录要应用的传入更改以及需要决定的冲突。
这里不会更改实时文件：`mise dot pull` 会执行此操作。
在 `fetch-only` 模式下不会发布任何内容。

历史监视器会在 `sync` 和 `fetch-only` 模式下自行执行此操作
（`settings.history.sync`）；此命令用于立即执行。

## 标志
- **`--fetch-only`** — 仅获取，不发布
- **`--allow-plaintext-history`** — 允许发布加密文件较旧的未加密版本
- **`--best-effort`** — 当无法访问 origin 时发出警告而不是失败
- **`-h --help`** — 打印帮助

<!-- generated reference navigation -->

## 相关文档

- [入门](/getting-started.html)。
- [`mise dotfiles <SUBCOMMAND>`](/cli/dotfiles.html)。
- [全局标志和参数语法](/cli/#global-flags)。
